"use client";
import { Eye, EyeOff, LogIn } from "lucide-react";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { XaadirLogo } from "@/components/brand/XaadirLogo";
import { authenticate } from "@/lib/auth";
import { useAuth } from "@/components/providers/AuthProvider";
export default function LoginPage() { const router=useRouter(); const {signIn}=useAuth(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [show,setShow]=useState(false); const [error,setError]=useState(""); function submit(e:React.FormEvent){e.preventDefault();let session; try { session=authenticate(email,password); } catch (error) { setError(error instanceof Error ? error.message : "Unable to sign in."); return; }if(!session){setError("Those demo credentials are not recognised.");return;}try { signIn(session); } catch { setError("Browser storage is unavailable. Allow site storage to keep your login, then try again."); return; }router.replace(session.role === "ADMIN" ? "/admin" : "/teacher");} return <main className="login-page"><div className="login-brand"><XaadirLogo /></div><section className="login-card"><span className="login-eyebrow">Xaadir workspace</span><h1>Welcome back</h1><p>Sign in to manage your school day.</p><form onSubmit={submit}><label>Email<input type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@xaadir.edu" required /></label><label>Password<div className="password-field"><input type={show?"text":"password"} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password" required /><button type="button" aria-label={show?"Hide password":"Show password"} onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<p className="login-error" role="alert">{error}</p>}<button className="login-submit" type="submit"><LogIn/> Sign In</button></form></section></main>; }
